package at.nipe.playlegend.playlegendbans.shared.config;

/**
 * Data object that stores the needed variables from the config
 *
 * @author NoSleep - Nipe
 */
public record Config(String url, String user, String password) {
}
